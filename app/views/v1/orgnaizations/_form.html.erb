<%= form_with(model: v1_orgnaization, local: true) do |form| %>
  <% if v1_orgnaization.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(v1_orgnaization.errors.count, "error") %> prohibited this v1_orgnaization from being saved:</h2>

      <ul>
      <% v1_orgnaization.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
