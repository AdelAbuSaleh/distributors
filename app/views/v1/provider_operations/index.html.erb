<p id="notice"><%= notice %></p>

<h1>V1 Provider Operations</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @v1_provider_operations.each do |v1_provider_operation| %>
      <tr>
        <td><%= link_to 'Show', v1_provider_operation %></td>
        <td><%= link_to 'Edit', edit_v1_provider_operation_path(v1_provider_operation) %></td>
        <td><%= link_to 'Destroy', v1_provider_operation, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New V1 Provider Operation', new_v1_provider_operation_path %>
