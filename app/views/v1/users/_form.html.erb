<%= form_with(model: v1_user, local: true) do |form| %>
  <% if v1_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(v1_user.errors.count, "error") %> prohibited this v1_user from being saved:</h2>

      <ul>
      <% v1_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
